---
phase: 03-deploy-cicd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
autonomous: true

must_haves:
  truths:
    - "Push to main triggers the deploy workflow automatically"
    - "Workflow SSHs to VPS and runs the deploy script"
    - "Workflow can also be triggered manually (workflow_dispatch)"
    - "Workflow fails visibly if deploy script exits non-zero"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions workflow for Docker-based deploy"
      contains: "deploy.sh"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "scripts/deploy.sh"
      via: "SSH remote execution"
      pattern: "deploy\\.sh"
---

<objective>
Update the GitHub Actions workflow to work correctly with the new Docker-based deploy script.

Purpose: The existing workflow at .github/workflows/deploy.yml already uses appleboy/ssh-action to SSH into the VPS and run scripts/deploy.sh. It needs minor updates to ensure compatibility with the Docker-based deploy (correct working directory, script timeout, environment expectations).

Output: An updated .github/workflows/deploy.yml that triggers on push to main and runs the Docker deploy.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/deploy.yml
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Update .github/workflows/deploy.yml. The current workflow is close to what we need but requires these changes:

1. Keep the existing trigger config (workflow_dispatch + push to main) — this is correct
2. Keep using `appleboy/ssh-action@v1.2.0` — this is correct
3. Keep the existing secrets (VPS_HOST, VPS_USER, VPS_SSH_KEY) — these are correct
4. Update the `script` block to:
   a. Add `script_stop: true` to the appleboy action's `with` block — this makes the action fail if any command in the script fails (respects set -e in deploy.sh)
   b. Add `command_timeout: 10m` to the appleboy action's `with` block — Docker builds can take several minutes, default 10s timeout is too short
   c. Keep the script as: `cd /root/NPS-Agro && ./scripts/deploy.sh` (same as current, just ensure it's on a single logical command)

The workflow should remain simple — all logic lives in deploy.sh, the workflow just triggers it via SSH.

Do NOT:
- Add Docker build steps in the workflow itself (builds happen on VPS via deploy.sh)
- Add environment variable injection (the VPS has its own .env file)
- Add multiple jobs or complex matrix builds
- Remove workflow_dispatch trigger
- Change the secrets names (VPS_HOST, VPS_USER, VPS_SSH_KEY are already configured)
  </action>
  <verify>Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"`. Verify the file contains script_stop and command_timeout settings.</verify>
  <done>.github/workflows/deploy.yml triggers on push to main, SSHs to VPS with proper timeout, runs deploy.sh, and fails the workflow if deploy fails.</done>
</task>

</tasks>

<verification>
- YAML is valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"`
- File triggers on push to main: `grep "main" .github/workflows/deploy.yml`
- File uses SSH action: `grep "appleboy/ssh-action" .github/workflows/deploy.yml`
- File has timeout: `grep "command_timeout" .github/workflows/deploy.yml`
- File has script_stop: `grep "script_stop" .github/workflows/deploy.yml`
- File references deploy.sh: `grep "deploy.sh" .github/workflows/deploy.yml`
</verification>

<success_criteria>
GitHub Actions workflow is configured to SSH-deploy to VPS on every push to main, with proper timeout for Docker builds and error propagation from the deploy script.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deploy-cicd/03-02-SUMMARY.md`
</output>
