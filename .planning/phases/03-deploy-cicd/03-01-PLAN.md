---
phase: 03-deploy-cicd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy.sh
autonomous: true

must_haves:
  truths:
    - "deploy.sh pulls latest code, builds Docker images, and starts containers"
    - "Migrations run automatically as part of container startup (not the deploy script)"
    - "Deploy script validates health after containers are up"
    - "Old PM2 references are completely removed"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "Docker-based deploy script replacing PM2 deploy"
      contains: "docker compose"
  key_links:
    - from: "scripts/deploy.sh"
      to: "docker-compose.yml"
      via: "docker compose up --build"
      pattern: "docker compose.*up"
    - from: "scripts/deploy.sh"
      to: ".env"
      via: "env file check before deploy"
      pattern: "\\.env"
---

<objective>
Rewrite the deploy script from PM2-based to Docker Compose-based deployment.

Purpose: The existing scripts/deploy.sh uses PM2 (npm install, npm build, pm2 restart). Phase 2 Dockerized everything, so deploy now needs to pull code, build images, and bring up containers via docker compose. Migrations already run inside the backend container on startup (run-migrations.sh && node dist/main.js), so the deploy script just needs to orchestrate the Docker lifecycle.

Output: A rewritten scripts/deploy.sh that does a full Docker-based deploy.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.yml
@scripts/deploy.sh
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite deploy.sh for Docker Compose</name>
  <files>scripts/deploy.sh</files>
  <action>
Rewrite scripts/deploy.sh to replace the PM2-based deploy with Docker Compose. The script must:

1. Keep `set -euo pipefail` and ROOT_DIR detection (lines 1-4 of current script)
2. Check that `.env` exists in ROOT_DIR (not `.env.production` — docker-compose.yml uses `env_file: .env`). Print clear error if missing.
3. Step [1/4] — Pull latest code: `git pull origin main` (keep the existing HEAD check guard from current script lines 21-25)
4. Step [2/4] — Stop existing containers gracefully: `docker compose down --timeout 30`
5. Step [3/4] — Build and start all services: `docker compose up -d --build`
   - The `--build` flag rebuilds images with latest code
   - The `-d` flag runs detached
   - Migrations run automatically inside the backend container (its command is `sh -c "./scripts/run-migrations.sh && node dist/main.js"`)
6. Step [4/4] — Health check validation: Wait for http://127.0.0.1/health (port 80, through nginx) to return 200
   - Use the same retry loop pattern as current script (15 attempts, 2s sleep)
   - Use `curl -fsS` for the check
   - On failure, print `docker compose logs --tail=50` for debugging, then exit 1

Do NOT:
- Reference PM2 anywhere
- Reference `.env.production` (the Docker setup uses `.env`)
- Run migrations in the script (they run inside the backend container)
- Run `npm install` or `npm build` (Docker handles this)
- Include `APP_NAME` variable (PM2 artifact)
  </action>
  <verify>Run `bash -n scripts/deploy.sh` to verify no syntax errors. Grep for "pm2" and "env.production" to confirm they are gone. Grep for "docker compose" to confirm Docker commands are present.</verify>
  <done>scripts/deploy.sh is a valid bash script that: checks .env exists, pulls code, runs docker compose down then up --build -d, and validates health at port 80. No PM2 references remain.</done>
</task>

</tasks>

<verification>
- `bash -n scripts/deploy.sh` exits 0 (no syntax errors)
- `grep -c "pm2\|PM2\|ecosystem" scripts/deploy.sh` returns 0
- `grep -c "docker compose" scripts/deploy.sh` returns at least 2 (down + up)
- `grep -c "\.env\.production" scripts/deploy.sh` returns 0
- Script checks for `.env` (not `.env.production`)
</verification>

<success_criteria>
scripts/deploy.sh is a complete Docker-based deploy script that can be called by GitHub Actions (or manually) to deploy the full stack on the VPS.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deploy-cicd/03-01-SUMMARY.md`
</output>
